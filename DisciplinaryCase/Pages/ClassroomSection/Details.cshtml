@page
@model DisciplinaryCase.Pages.ClassroomSection.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>اطلاعات</h1>

<div>
    <h4>Classroom</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Classroom.Name)
        </dt>
        <dd class="col-sm-10">
            اسم کلاس
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Classroom?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
